CREATE TABLE BILLABLE_CHARGE
   (BILLABLE_ITEM_ID VARCHAR2(64),
	BILLABLE_CHARGE_ID CHAR(12),
	SUB_ACCOUNT_ID CHAR(10),
	START_DT DATE,
	PRICEITEM_CD CHAR(30),
	PRICE_ASGN_ID CHAR(10),
	SETTLEMENT_LEVEL_TYPE VARCHAR2(60),
	SETTLEMENT_GRANULARITY VARCHAR2(60),
	CURRENCY_FROM_SCHEME VARCHAR2(60),
	FUNDING_CURRENCY VARCHAR2(60),
	MERCHANT_AMOUNT_SIGNAGE NUMBER(36,18),
	SETT_LEVEL_GRANULARITY VARCHAR2(30),
	CHILD_PRODUCT VARCHAR2(20),
	PARENT_ACCT_ID CHAR(10),
	CRE_DTTM TIMESTAMP (6) NOT NULL ,
	BATCH_CODE VARCHAR2(128) NOT NULL ,
	BATCH_ATTEMPT NUMBER(3,0) NOT NULL ,
	PARTITION_ID NUMBER(*,0) NOT NULL ,
	ILM_DT TIMESTAMP (6) NOT NULL ,
	ILM_ARCH_SW CHAR(1) NOT NULL ,
	AGGREGATION_HASH VARCHAR2(256),
	PARTITION NUMBER(5,0) GENERATED ALWAYS AS (ORA_HASH(BILLABLE_ITEM_ID,127)) VIRTUAL ,
	BILLING_CURRENCY CHAR(3),
	LEGAL_COUNTERPARTY CHAR(5),
	PRICE_CURRENCY CHAR(3),
	TXN_CURRENCY CHAR(3)
    );