INSERT INTO post_bill_adj_accounting (
    party_id,
    acct_id,
    acct_type,
    sub_acct_id,
    sub_acct_type,
    currency_cd,
    bill_amt,
    bill_id,
    post_bill_adj_id,
    amount,
    lcp,
    business_unit,
    class,
    type,
    bill_dt,
    cre_dttm,
    batch_code,
    batch_attempt,
    ilm_dt,
    ilm_arch_sw,
    partition_id
) VALUES (
             :party_id,
             :acct_id,
             :acct_type,
             :sub_acct_id,
             :sub_acct_type,
             :currency_cd,
             :bill_amt,
             :bill_id,
             :post_bill_adj_id,
             :amount,
             :lcp,
             :business_unit,
             'POST_BILL_ADJ',
             :type,
             :bill_dt,
             SYSTIMESTAMP,
             :batch_code,
             :batch_attempt,
             :ilm_dt,
             'Y',
             :partition_id
         )