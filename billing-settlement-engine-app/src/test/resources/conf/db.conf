pricing {
  driver: oracle.jdbc.driver.OracleDriver
  url: "${database.url}"
  user: CBE_PCE_APPUSER
  password: "${database.password}"
}

chargeUpload {
  driver: oracle.jdbc.driver.OracleDriver
  url: "${database.url}"
  user: CBE_CUE_APPUSER
  password: "${database.password}"
}

merchantData {
  driver: oracle.jdbc.driver.OracleDriver
  url: "${database.url}"
  user: CBE_MDU_APPUSER
  password: "${database.password}"
}

cisadm {
  driver: oracle.jdbc.driver.OracleDriver
  url: "${database.url}"
  user: CISADM
  password: "${database.password}"
}

ref {
  driver: oracle.jdbc.driver.OracleDriver
  url: "${database.url}"
  user: CBE_REF_APPUSER
  password: "${database.password}"
}

user {
  driver: oracle.jdbc.driver.OracleDriver
  url: "${database.url}"
  user: "${database.username}"
  password: "${database.password}"
}